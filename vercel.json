{
  "buildCommand": "git clone https://github.com/flutter/flutter.git --depth 1 --branch 3.29.2 flutter_sdk && ./flutter_sdk/bin/flutter config --enable-web && ./flutter_sdk/bin/flutter pub get && ./flutter_sdk/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}